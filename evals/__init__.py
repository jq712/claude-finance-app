"""Milestone 6 agent-eval assets: the golden financial dataset and eval
case definitions. Kept outside `tests/` per handoff §27's layout — the
pytest harness that runs these lives in `tests/agent_evals/` and imports
from here."""
